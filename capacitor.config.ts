import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pl.englishapp.mobile',
  appName: 'English App',
  webDir: 'dist/english-app/browser'
};

export default config;
